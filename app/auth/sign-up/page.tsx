'use client'
import Link from 'next/link'
import { FormEvent, useState } from 'react'
import { ArrowLeft, ArrowUpRight, UserRound } from 'lucide-react'
import { signUp } from '@/lib/supabase/client'

export default function SignUpPage(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage('');const {error}=await signUp(email,password,name);setMessage(error?'We could not create your account. Check your details and try again.':'Account created. Check your email to confirm access.');setLoading(false)}return <main className="grid min-h-screen place-items-center bg-muted/30 p-6"><div className="w-full max-w-md rounded-2xl border border-border bg-background p-7 sm:p-9"><Link href="/auth/login" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/> Back to sign in</Link><div className="mt-10"><div className="grid size-11 place-items-center rounded-xl bg-primary/10 text-primary"><UserRound className="size-5"/></div><h1 className="mt-5 text-3xl font-semibold tracking-tight">Request workspace access</h1><p className="mt-2 text-muted-foreground">Create an account for your assigned Greenline role.</p></div><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-medium">Full name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 outline-none focus:ring-3 focus:ring-ring/30" placeholder="Your name"/></label><label className="block text-sm font-medium">Work email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 outline-none focus:ring-3 focus:ring-ring/30" placeholder="you@company.com"/></label><label className="block text-sm font-medium">Password<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 h-11 w-full rounded-lg border border-input px-3 outline-none focus:ring-3 focus:ring-ring/30" placeholder="At least 8 characters"/></label>{message&&<p className="rounded-lg bg-primary/10 p-3 text-sm text-primary">{message}</p>}<button disabled={loading} className="inline-flex h-11 w-full items-center justify-center gap-2 rounded-lg bg-primary font-medium text-primary-foreground transition-transform duration-150 hover:bg-primary-hover active:scale-[.97] disabled:opacity-60">{loading?'Creating…':'Create account'}<ArrowUpRight className="size-4"/></button></form><p className="mt-7 text-center text-sm text-muted-foreground">Already have access? <Link href="/auth/login" className="font-medium text-primary hover:underline">Sign in</Link></p></div></main>}
